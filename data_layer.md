# 数据层规范（Data Layer）

数据层负责符号流与字节流之间的双向转换：多符号联合打包、RS 纠错。

数据层**不感知帧结构、frame_type、payload 语义**——这些由传输层定义。数据层只接收 byte 流，输出符号流（编码方向），或接收符号流输出 byte 流（解码方向）。

**上层**：传输层
- 编码方向：接收 byte 流（长度 L ≤ L_max），打包为符号流
- 解码方向：交付 byte 流（RS 纠错后）

**下层**：符号层
- 编码方向：交付符号流（恰好 S 个符号，每个值 0 到 M-1）
- 解码方向：接收符号流（S 个）+ 置信度（erasure 标记）

**容量参数**（类比网络 MTU，各层公布自己的能力，上层来查）：
- 符号层公布 S（单帧符号容量，由网格等级决定）和 M（符号总数，由颜色档/图形档决定）
- 数据层公布 L_max（单帧可承载的 data byte 容量，由 S、M、打包参数、RS 参数决定）
- 传输层查 L_max，据此控制 payload 长度

---

## 0. 符号

### Specification

- **符号（Symbol）** 是一个非负整数，值域为 [0, M-1]
- M 为符号总数（符号空间的基数），由符号层决定
- 数据层只接收 M 值，不感知 M 的来源
- 符号流（symbol sequence）是一串符号的有序序列

---

## 0.1 容量

### Specification

数据层的容量 L_max（单帧可承载的 data byte 数）由 S、M、打包参数 (k, n)、RS(255, 223) 决定，是固定常量（给定等级和档位后即可查表）。

```
打包块数 P = floor(S / k)
打包产生 byte 数 = P × n
RS 块数 R = floor(P × n / 223)
L_max = R × 223
```

约束：
- 传输层交付的 byte 流长度 L ≤ L_max
- 若 L < L_max，传输层负责 padding 至 L_max
- S 不一定能被 k 整除：末尾不足 k 个的符号填充固定值（如 0）

### 各等级容量示例（M=24, k=3, n=2）

| 等级 | S | P=floor(S/3) | P×n (bytes) | R=floor(P×n/223) | L_max=R×223 |
|------|---|-------------|-------------|------------------|-------------|
| 1 | 211 | 70 | 140 | 0 | 0 |
| 2 | 595 | 198 | 396 | 1 | 223 |
| 3 | 1107 | 369 | 738 | 3 | 669 |
| 4 | 1747 | 582 | 1164 | 5 | 1115 |

> 等级 1 容量为 0：140 bytes 不足以填满一个 RS(255,223) 块。低等级需用更小的 RS 参数或更高 M。

### Proposal

- L_max 是数据层的能力属性（类比 MTU），给定等级和档位后是常量，传输层直接查表
- padding 由传输层负责（数据层按固定长度 L_max 处理）

---

## 1. 多符号联合打包

### Specification

- 符号流为 M-进制
- M 通常不是 2 的幂，无法直接按 bit 对齐
- 将 k 个 M-进制符号视为一个 base-M 大整数，转换为 n bytes（RS 符号）
- 选择最优 k，使 k 个符号的 base-M 范围最接近 n×8 bits

### 打包规则

```
value = s[0] × M^(k-1) + s[1] × M^(k-2) + ... + s[k-1] × M^0
```

- value 转为 n bytes（big-endian）
- 要求 M^k ≤ 256^n（value 不溢出）

### 解包规则

```
value = bytes_to_int(n bytes)
for i in 0..k-1:
    s[k-1-i] = value % M
    value = value // M
```

### 各档位参数

M 由符号层决定（M = C(颜色数,2) × 图形数），数据层只接收 M 值，不感知其来源。

| M | k（符号数） | n（bytes） | M^k | 256^n | 效率 |
|---|------------|-----------|-----|-------|------|
| 4 | 4 | 1 | 256 | 256 | 100% |
| 16 | 2 | 1 | 256 | 256 | 100% |
| 24 | 3 | 2 | 13824 | 65536 | 87.5% |
| 96 | 2 | 2 | 9216 | 65536 | 70.3% |
| 112 | 2 | 2 | 12544 | 65536 | 78.4% |
| 448 | 2 | 3 | 200704 | 16777216 | 75.9% |

- 效率 = log2(M^k) / (n × 8)
- M = 2^m 时效率 100%（如 M=4、M=16）

### Proposal

- 多符号联合打包而非逐符号转 bits，是因为 M 非 2 的幂时逐符号转换浪费大
- 选择 k 使 M^k 最接近 256^n，最大化 byte 利用率
- 单个符号错误会扩散到所在打包块的 n bytes，但 RS 按 byte 纠错，影响可控

---

## 2. Reed-Solomon 纠错

### Specification

- RS 码工作在 GF(256) 上，每个 RS 符号 = 1 byte
- 打包后的 byte 流按 RS(255, 223) 分块纠错，统一参数
- 数据层根据已知 byte 长度自行分块，无需外部参数

| 参数 | 值 |
|------|-----|
| RS 码 | RS(255, 223) on GF(256) |
| 纠错能力 | 每 255 bytes 纠正 16 bytes 错误 |
| 纠错码占比 | ~14% |

### erasure 机制

- 符号层可输出符号置信度，低置信度符号标记为 erasure
- 打包块内含 erasure 符号 → 整块标记为 erasure（n bytes 位置已知）
- RS 纠错：erasure 1:1（1 个纠错 byte 纠 1 个 erasure byte），error 1:2
- erasure 使 RS 纠错能力翻倍

### Proposal

- 使用标准 GF(256) 而非 GF(2^m)，是因为 GF(256) 库实现成熟、byte 对齐、块长度 255 足够大
- erasure 来自符号层置信度，不依赖值范围判断
- RS 统一参数，数据层自包含：编码方向按已知 byte 长度分块加 RS，解码方向按已知符号数推算 byte 长度分块纠错
- Header 帧的高可靠不靠更高 RS 冗余，靠传输层重复发送（周期性广播）

---

## 3. 处理顺序

### 编码方向（byte → 符号）

```
1. 传输层交付 L bytes（L ≤ L_max，不足则 padding 至 L_max）
2. RS 编码：L_max bytes → R × 255 bytes（每 223 data + 32 parity）
3. 多符号打包：R × 255 bytes → floor(R×255 / n) × k 个符号
   - 不足一个打包块的末尾 bytes 填充 0 后打包
4. 符号填充：若符号数 < S，末尾补 0 符号至 S 个
5. 交付符号层：S 个符号
```

### 解码方向（符号 → byte）

```
1. 符号层交付 S 个符号 + 置信度
2. 多符号解包：floor(S/k) 个打包块 → floor(S/k) × n bytes
   - 含 erasure 的打包块标记对应 n bytes 为 erasure
3. RS 解码：按 255 bytes 分块，每块纠错输出 223 data bytes
4. 交付传输层：R × 223 bytes（传输层根据 payload 语义截断至原始 L）
```

### Proposal

- 编码方向：先 RS 再打包（RS 作用于完整 byte 流，打包适配符号数）
- 解码方向：先解包再 RS 解码（恢复 byte 流后纠错）
- 末尾不足部分用 0 填充，解码后由传输层截断
